 <style>

/* @media screen and (max-width:700px) {
    .product_img{
        width: 900x;
        height: 500px;
    }
} */

 </style>
 
 
 <div class="w-full py-6 px-3">
                <div class="overflow-hidden ">
                    <a href="{{product_product.url}}">
                        {% if product_product.featured_image %}
                            <div class="overflow-hidden">
                               <img class="rounded object-cover h-full transition-none lg:transition duration-1000 ease-in-out hover:scale-105 product_img" src="{{product_product.featured_image | img_url: 'large'}}" alt="{{product_product.featured_image.alt}}">
                            </div>

                            {% else %}
                                {{ 'product-1' | placeholder_svg_tag: 'w-full h-full object-cover bg-gray-100'}} 
                        {% endif %}
                    </a>

                        <div class="flex flex-col py-4">
                            <a href="{{product_product.url}}" class="font-medium text-pink-200 rounded hover:text-white">{{product_product.title}}</a>
                            <p>{{product_product.price  | money}}</p>    
                        </div>
                
                
                </div>
        </div>
        